# Cài đặt

- nodejs: version >16
- docker
- makefile

# Backend

Khi chạy lần đầu tiên

    make bootstrap

hoặc

    npm install
    docker-compose up -d
    npm run seed
    npm run start:dev

# Frontend

    npm install
    npm run dev
