export enum StatusType {
  PENDING = 'PENDING',
  CONFIRMED = 'CONFIRMED',
  CANCELLED = 'CANCELLED',
  DONE = 'DONE',
}

export enum TimePeriod {
  MORNING = 'MORNING',
  AFTERNOON = 'AFTERNOON',
  EVENING = 'EVENING',
}

export enum Gender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
  OTHER = 'OTHER',
}

export enum Role {
  ADMIN = 'ADMIN',
  DOCTOR = 'DOCTOR',
  PATIENT = 'PATIENT',
}

export enum SearchType {
  DOCTOR = 'DOCTOR',
  HOSPITAL = 'HOSPITAL',
  SPECIALTY = 'SPECIALTY',
  ALL = 'ALL',
}
